# STADVDB_S11.7_T120-21
## Running the app locally
1. For the latest version, checkout to the `dev` branch: `git checkout dev`
1. Run `npm install`
1. Run `npm run build:js`
1. Run `npm run start`

You should see the message `App listening at port 3000`.
